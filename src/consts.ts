// Угол 60 градусов в радианах (любой угол гексагона)
export const angle60inRadian = (2 * Math.PI) / 6;// const overlayCanvas = document.getElementById("overlayCanvas");
export const initialHexRadius = 50;
export const maxHexRadius = 100;
export const minHexRadius = 25;

